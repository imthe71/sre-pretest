題目一：找出出現次數最多的單字

這支程式會讀取 words.txt，找出裡面出現最多次的單字。

做法很直接：先把整份文字讀進來，把單字取出來，再把英文字統一轉成小寫後計算出現次數。這樣 Twinkle 和 twinkle 會被算成同一個字；逗號、句點、驚嘆號這些標點不會影響結果。

題目的範例裡，twinkle 一共出現四次，所以輸出會是：

4 twinkle

執行方式：

python wordfreq.py words.txt
